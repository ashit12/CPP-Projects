CompileFlags:
  Add: [-std=c++20]          # fallback when compile_commands.json is absent
  Remove: [-W*]              # strip noisy driver warnings clangd can't map

Diagnostics:
  UnusedIncludes: Strict     # flag headers included but not needed
